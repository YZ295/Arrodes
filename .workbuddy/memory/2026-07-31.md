## 2026-07-31 阿罗德斯项目优化

### 深度学习
- 完整阅读了 Plan 文件夹全部 28 份架构文档（总纲/P0-P3/Now0-Now8）
- 审查了 52 个源文件（7152 行代码），client 32 文件 + server 20 文件
- 理解项目定位：v3.0 多模态 AI 智能体，语音+3D宇宙+记忆+视觉

### 代码优化（6 项已完成）

1. **消除 uid() 重复** - 提取到 shared/utils/uid.ts
2. **消除 TTS 双轨制** - useVoiceChat 改用 useTTS hook（原为内联 speakText）
3. **创建统一 API 客户端** - apiClient.ts 替代分散的 15+ 个 raw fetch()
4. **拆分 VoiceDialog** - 提取 ModelSettings.tsx + SessionPanel.tsx（438→270行）
5. **服务端配置解耦** - config.ts 去除硬编码 Hermes 路径
6. **添加输入验证** - validateBody 中间件 + session 路由验证

### 验证
- client/ tsc --noEmit: 零错误
- server/ tsc --noEmit: 零错误
- 生成优化报告 Plan/Now/Now9_优化报告_v1.0.md

### 后续建议
- P0: useVoiceChat WS 层迁移到 MessageChannel
- P1: EventBus 泛型类型安全
- P2: 请求日志 + 限流中间件

### v4.0 架构升级（基于 Now9 继续开发）

**Phase A: WS 统一** — useVoiceChat.ts 迁移到 MessageChannel（541→290行，-46%）
- 原始 WebSocket 管理（~120行连接/重连/断开）→ MessageChannel 单例
- handleServerMessage switch → onChunk/onComplete/onMemory/onError 回调
- wsRef.send → channel.send(), readyState → isConnected()

**Phase B: AIRI 管道架构** — 新建 Pipeline.ts + shared/types/pipeline.ts
- 定义 6 阶段管道：stt → intent → memory_recall → llm → memory_save → tts
- PipelineRunner 支持：超时保护、错误跳过、性能追踪、生命周期回调
- 管道上下文 PipelineContext 贯穿所有阶段

**Phase C: 插件系统** — 新建 PluginManager.ts + shared/types/plugin.ts
- ArodesPlugin 接口：manifest + hooks (onBeforeMessage/onAfterMessage/onCommand等)
- PluginManager：注册/卸载/激活/停用，钩子链串联，错误隔离
- 内置 Logger 插件

**新建文件**: shared/types/pipeline.ts, shared/types/plugin.ts, client/src/core/Pipeline.ts, client/src/core/PluginManager.ts, Plan/Now/Now10_v4.0开发进展.md

**验证**: client tsc ✅, server tsc ✅

**待办**: (1) Rust 安装 + Tauri 初始化 (2) PipelineRunner 集成到实际对话流程 (3) PluginManager 接入 useVoiceChat

### v4.1 管道接入 + 插件激活（基于 Now10 确认）

**管道接入 useVoiceChat**:
- 创建 4 个阶段处理器：intentStage / llmStage / ttsStage / memoryStage
- 创建 voicePipeline.ts 组装完整管道 + PluginManager 钩子集成
- useVoiceChat.sendMessage() 普通消息由 pipeline.run() 编排
- LLM 阶段：通过 MessageChannel 发送，Promise 等待 complete（chunks 仍走回调更新 UI）
- TTS 由管道 tts 阶段处理，handleComplete 只更新 UI 不再播放语音

**PluginManager 激活**:
- App.tsx 启动时激活内置 Logger 插件
- Pipeline 执行前后自动调用 beforePipeline/afterPipeline 钩子

**新建文件**: pipeline/stages/{intent,llm,tts,memory}Stage.ts, pipeline/stages/index.ts, pipeline/voicePipeline.ts, Plan/Now/Now11_v4.1管道接入.md

**修改**: useVoiceChat.ts (集成 pipeline.run), App.tsx (初始化 PluginManager)

**验证**: client tsc ✅, server tsc ✅

**下一步**: 启动前后端端到端验证，查看管道日志输出

### v4.2 语音系统蒸馏（基于 AIRI 架构）

**蒸馏 AIRI 语音流水线**:
- AudioContextManager: 全局单例 AudioContext，suspend/resume/close 生命周期管理
- useVAD: 语音活动检测 hook（AnalyserNode RMS + 帧状态机，替代手动按键）
- TtsEngineRegistry: 可插拔 TTS 引擎（Edge TTS priority 0, Web Speech priority 1，自动降级链）
- SttEngineRegistry: 可插拔 STT 引擎（Web Speech，预留 Whisper）
- AudioVisualizer: Canvas 实时波形/频谱/环形三模式可视化（60fps）
- useAudioRecorder 增强: 降噪/AGC/回声消除 + 时长追踪 + 音量反馈

**UI 集成**:
- VoiceDialog 录音时显示红色频谱条 + 时长
- VoiceDialog TTS 播放时显示金色波形动画
- App.tsx 初始化 TTS/STT 引擎注册表

**新建文件**: modules/voice/{AudioContextManager,useVAD,TtsEngineRegistry,SttEngineRegistry}.ts, components/AudioVisualizer.tsx, Plan/Now/Now12_v4.2语音系统蒸馏.md

**修改**: useAudioRecorder.ts (增强), useVoiceChat.ts (导出 recordingDuration/Volume), VoiceDialog.tsx (集成可视化), App.tsx (初始化注册表)

**验证**: client tsc ✅, server tsc ✅

### v4.3 语音消失修复 + 单会话模式 [20:47]
- 语音消失: finalTextRef 替代 state 闭包, sttPromiseRef await STT Promise
- 单会话: 移除多 session 管理, VoiceDialog 简化为纯对话面板
- 记忆隔离: 不再加载旧会话消息历史
- 修改: useSpeechToText.ts, useVoiceChat.ts, VoiceDialog.tsx

### v4.4 TTS + Avatar + 多会话恢复 [20:56]
- Avatar.tsx: THE FOOL 愚者形象, 36px 标题栏头像, 说话时金色光晕
- useTTS v4.4: 持久 audio 元素 DOM, autoplay 解锁 unlockAudio(), 错误可见 + 重播
- useVoiceChat v4.4: 多会话管理恢复, switchSession/createNewSession 导出
- VoiceDialog: 会话按钮恢复 + Avatar 标题栏

### v4.5 音波粒子 + TTS 设置修复 [21:07]
- useAudioLevelStore: 共享音频电平 (inputLevel/outputLevel/mode)
- useAudioRecorder: AnalyserNode 真实音量分析写入 store
- useTTS: MediaElementSource → AnalyserNode → destination 音频图, 输出电平写入 store
- AudioParticles.tsx: 800 粒子球面分布, 录音红色/TTS 金色脉动
- HomePlanet 集成 AudioParticles
- TTS 设置修复: VoiceDialog 不再用本地 state, 改走 useVoiceChat → useTTS.setConfig 实时生效

### v5.0 大改版 [21:27]
- TTS Analyser 修复: unlockAudio 时建立 MediaElementSource 音频图, onplay 不再 async 等待
- Sidebar.tsx: 可折叠左侧导航 (对话/工作流/画像/记忆/视觉/配置/移动端/高级)
- HomePlanet v2: 赛博朋克 (暗核+青色线框+电路纹理+全息环+扫描线)
- ChatOverlay.tsx: 移除浮动对话框, AI字幕在星球上方, 用户输入在下方, 底部极简输入栏
- PanelView.tsx: 侧边栏面板路由 (Profile/Memory/Vision/Settings/Placeholder)
- App.tsx v5: Sidebar + Universe + ChatOverlay/PanelView 三层布局

### v5.1 核心能力补齐 [22:15]
**MemoryGateway**:
  - retrieveContext: 记忆检索 + 画像注入 + 摘要, 合并为统一上下文
  - processConversation: LLM分析→提取记忆→存储→更新画像JSON
  - user_profile.json: 自动积累的画像 (facts/preferences/interests/events/tasks)
**Skills系统**:
  - registry.ts: AgentSkill接口 + 注册表 + 解析+执行, tool_call标签协议
  - builtin.ts: 5个技能 (search_memory/get_profile/get_time/create_session/system_info)
  - WS handler: Agent Loop (LLM回复→解析tool_call→执行→结果注入→再次LLM, 最大3轮)
- LlmService: 新增chatSimple(非流式)和chatStreamSimple(流式简版)
- 字幕修复: 移除char-glitch类消除黑影遍历
